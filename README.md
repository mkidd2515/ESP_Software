## how to configure

you need to install pyenv or be running python 3.11.x or lower, then install mbed-cli in that pyenv.
Make sure that when you do "mbed ls" your version is 6.17. Then do

`mbed target NUCLEO_F401RE`

`mbed toolchain GCC_ARM`

you will need to install `none-eabi-gcc`




